{
  "name": "root",
  "private": true,
  "scripts": {
      "cache": "npx lerna add-caching",
      "graph": "npx nx graph",
      "build": "npx lerna run build",
      "test": "npx lerna run test",
      "dev:remix": "npx lerna run dev --scope=remixapp"
  },
  "devDependencies": {
    "lerna": "^6.0.3"
  },
  "workspaces": [
    "packages/**"
  ]
}
